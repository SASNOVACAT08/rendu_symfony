{% extends 'base.html.twig' %}

{% block title %}{{'catch'|trans}}{% endblock %}

{% block body %}
    <h1 class="">{{cour.title}}</h1>
    <p>{{cour.content}}</p>

    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('cours_edit', {'slug': cour.slug}) }}">{{'edit'|trans}}</a>

        {{ include('cours/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
