<form method="post" action="{{ path('cours_delete', {'slug': cour.slug}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
    <button class="btn">{{'delete'|trans}}</button>
</form>
